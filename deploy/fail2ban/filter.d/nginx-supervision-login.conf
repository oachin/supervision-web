# Échecs de connexion et rate-limit sur /api/auth/login
[Definition]
failregex = ^<HOST> -.*"(POST) /api/auth/login HTTP[^"]*" (401|403|429|503)
            ^<HOST> -.*"(POST) /api/auth/login HTTP[^"]*" 400
ignoreregex =
