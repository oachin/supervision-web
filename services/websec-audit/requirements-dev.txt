# Development / test dependencies.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0,<10
ruff>=0.7,<0.13

# HTTP client used by fastapi.testclient for the dashboard tests
httpx>=0.27,<0.29
